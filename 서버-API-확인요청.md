# 스팟 검수 어드민 — 서버 API 확인 요청

> 보내는 쪽: 어드민(프론트) / 받는 쪽: 서버팀
> 근거 문서: "[기획] 스팟 검수용 어드민 페이지 (Next.js)" 8장, 어드민 개발계획 5장
> 현재 상태: 화면(목록·상세·승인/반려)은 목 데이터로 완성. **API 연동만 남았고, 아래 항목이 정해져야 착수 가능합니다.**

---

## 0. 가장 급한 것 3개

나머지는 붙여가며 물어봐도 되지만, 이 3개는 **화면 구조 자체가 바뀌어서** 먼저 필요합니다.

1. **목록 조회 API의 응답 샘플 JSON 한 벌** (아래 2번)
2. **정렬·검색·필터를 서버가 처리하는지 + 페이지네이션 유무** (아래 1번)
3. **"재검토대기"를 서버가 어떻게 표현하는지** (아래 3번)

---

## 1. 목록 조회 — 정렬/검색/필터/페이징 주체

현재 어드민은 **전체 목록을 받아서 클라이언트에서 정렬·검색·필터**하고 있습니다.
서버가 처리하는 방식이면 해당 코드를 걷어내고 쿼리 파라미터 방식으로 다시 만들어야 합니다.

| 질문 | 확인 |
|---|---|
| 정렬을 서버가 처리하나요? | ☐ 서버 ☐ 클라이언트(전체 내려줌) |
| 상태 필터를 서버가 처리하나요? | ☐ 서버(`?status=`) ☐ 클라이언트 |
| 검색(스팟명·닉네임 부분일치)을 서버가 처리하나요? | ☐ 서버(`?q=`) ☐ 클라이언트 |
| **페이지네이션이 있나요?** | ☐ 있음(방식: ) ☐ 없음(전체 반환) |

**페이지네이션이 있으면 별도 협의가 필요합니다.** 기획서 4.1의 정렬 규칙이
"미처리 상단 → 재검토 우선 → 오래된 순 / 완료건은 최근 처리순"인데,
페이징이 걸리면 이 정렬을 **서버가 해줘야만** 합니다. (클라이언트는 현재 페이지 안에서만 정렬 가능)

---

## 2. 응답 스펙 — 샘플 JSON 요청

**목록 응답과 상세 응답 샘플 JSON을 한 벌씩 주시면 대부분 해결됩니다.**

어드민이 현재 기대하고 있는 필드는 아래와 같습니다. 필드명·타입이 다르면 알려주세요.

```jsonc
{
  "id": "1",
  "appliedAt": "2026-07-01T09:12:00+09:00",  // 신청일시
  "userNickname": "산책러",                    // 등록 유저
  "name": "노을 지는 한강 계단",                // 스팟명
  "status": "검수중",                          // 아래 3번 참고
  "handlerName": null,                         // 처리자 (미처리면 null)
  "handledAt": null,                           // 처리일시 (미처리면 null)

  // 상세 화면 필드 (기획서 4.2)
  "photoUrls": ["https://.../1.jpg"],
  "address": "서울특별시 ...",                  // 상세주소
  "comment": "해질 무렵에 가면 ...",            // 한줄코멘트
  "shotAt": "2026-07-01T08:40:00+09:00",       // 촬영일시
  "filters": ["노을", "산책"],

  // 과거 반려 이력 (최신순) — 재검토 건 상단에 표시
  "rejectionHistory": [
    {
      "reason": "사진 품질 미달",
      "detail": "기타 선택 시에만 채워짐",
      "handlerName": "김예은",
      "rejectedAt": "2026-06-29T11:00:00+09:00"
    }
  ]
}
```

### 2-1. 상세 조회 API가 따로 있나요?

- ☐ 있음 (경로: )
- ☐ 없음 → 이 경우 **목록 응답에 사진·상세주소·코멘트까지 전부 포함**되어야 합니다

### 2-2. 날짜 포맷

- 형식: ☐ ISO 8601 ☐ 기타( )
- 타임존: ☐ `+09:00` ☐ `Z`(UTC) ☐ 오프셋 없음
- **응답마다 섞이지 않고 하나로 통일되는지**가 중요합니다 (정렬에 직접 영향)

### 2-3. 사진 URL

- ☐ 영구 절대 URL ☐ 만료되는 presigned URL (만료시간: )
- **이미지 호스트 도메인**: (Next.js 설정에 등록이 필요합니다)
- 한 건당 최대 장수:

### 2-4. `filters` 값

- ☐ 자유 문자열 ☐ 정해진 코드 집합 (목록: )

---

## 3. 상태값 정의

### 3-1. `status`를 뭘로 내려주나요?

- ☐ 한국어 문자열 (`"검수중"` / `"재검토대기"` / `"승인"` / `"반려"`)
- ☐ 영문 코드 (예: `UNDER_REVIEW` / `RE_REVIEW_PENDING` / `APPROVED` / `REJECTED`)
- 실제 값 목록:

### 3-2. "재검토대기"를 서버가 별도 상태로 주나요?

기획서 8장의 **재신청 처리 방식** 미확정 항목과 같은 질문입니다.

- ☐ 서버가 `재검토대기` 상태로 내려줌
- ☐ 별도 상태 없음 → 어드민이 "반려 이력이 있는 검수중 건"으로 판별해야 함

### 3-3. 재신청이 들어오면 목록에서 어떻게 보이나요?

- ☐ 기존 건이 갱신됨 (같은 `id`, 상태만 재검토대기로)
- ☐ 신규 건으로 추가됨 (새 `id`) → **이 경우 같은 스팟이 목록에 여러 줄 뜨는데, 의도한 동작인지 확인 필요**

---

## 4. 승인 / 반려 API

현재 어드민이 가정하고 있는 규격입니다. 다르면 알려주세요.

```
POST /spots/{id}/approve
POST /spots/{id}/reject     body: { "reason": "...", "detail": "..." }
```

| 질문 | 확인 |
|---|---|
| 실제 경로·메서드 | 승인: / 반려: |
| 반려 사유를 뭘로 보내나요? | ☐ 한국어 문자열 ☐ 코드값(목록: ) |
| 응답으로 변경된 스팟 객체를 돌려주나요? | ☐ 예 ☐ 아니오(빈 응답) |

### 4-1. 반려 사유 5개가 서버 정의와 일치하나요?

기획서 5장 기준으로 어드민에 아래 5개가 들어가 있습니다. **서버가 다르게 정의했을 수 있어 대조가 필요합니다.**

1. 스팟으로 보기 어려움
2. 사진 품질 미달
3. 위치 정보 불일치
4. 중복 등록
5. 기타 → 추가 입력 필수 (어드민에서 미입력 시 확정 버튼 비활성)

"기타"의 추가 설명은 `detail` 필드로 보낼 예정입니다. 필드명이 다르면 알려주세요.
**서버에서도 "기타면 detail 필수" 검증을 하나요?** ☐ 예 ☐ 아니오

### 4-2. 처리자를 누가 식별하나요?

- ☐ 서버가 인증 토큰에서 식별 (권장)
- ☐ 어드민이 요청 바디에 담아 전송 (필드명: )

> 현재 로그인 미구현 상태라 처리자 이름이 임시로 하드코딩돼 있습니다. 이 항목이 정해지면 교체합니다.

---

## 5. 동시성 / 에러 처리 (기획서 7.2)

기획서상 "다른 팀원이 이미 처리한 건"을 눌렀을 때 안내 후 목록을 새로고침해야 합니다.

| 질문 | 확인 |
|---|---|
| 이미 처리된 건에 승인/반려를 호출하면? | ☐ 409 Conflict ☐ 다른 상태코드( ) ☐ 200으로 덮어씀 |
| 에러 응답 공통 포맷이 있나요? | 예: `{ "code": "ALREADY_HANDLED", "message": "..." }` |
| HTTP 상태코드만으로 케이스를 구분할 수 있나요? | ☐ 예 ☐ 아니오(바디의 code 필요) |

---

## 6. 인증 / 환경 (기획서 9장)

| 질문 | 확인 |
|---|---|
| 어드민 API에 인증이 필요한가요? | ☐ 필요 ☐ 불필요(화면단에서만 차단) |
| 필요하다면 방식은? | ☐ 구글 ID 토큰 검증 ☐ 서버 세션/JWT 발급 ☐ 기타 |
| 팀원 계정은 어떻게 관리하나요? | ☐ 어드민 하드코딩 화이트리스트 ☐ 서버 관리 |
| Base URL (dev / staging) | dev: / staging: |

---

## 7. 유저 신뢰도 (급하지 않음)

상세 화면에 자리만 잡아둔 상태라 **답변이 늦어도 진행에 지장 없습니다.**

| 질문 | 확인 |
|---|---|
| 제공 가능한가요? | ☐ 가능 ☐ 불가 ☐ 추후 |
| 가입일 | ☐ 가능 |
| 누적 등록 / 승인 / 반려 횟수 | ☐ 가능 |
| 전달 방식 | ☐ 상세 응답에 포함 ☐ 별도 API |

---

## 회신 요청

**0장의 3개만 먼저 주셔도 착수할 수 있습니다.** 나머지는 연동하면서 확인해도 됩니다.
가장 빠른 방법은 **목록/상세 응답 샘플 JSON + 1번 표 체크** 입니다.
