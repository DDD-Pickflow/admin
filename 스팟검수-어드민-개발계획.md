# 스팟 검수용 어드민 페이지 — 개발 계획 (Next.js)

> 대상: 웹 개발 경험 없는 iOS 개발자 / AI 활용 개발 전제
> 총 공수 목표: **약 15시간** (순조로울 때 기준, 막히면 18~20h)
> 기획서: "[기획] 스팟 검수용 어드민 페이지 (Next.js)" — 김예은

---

## 0. iOS ↔ 웹 개념 매핑 (먼저 감 잡기)

| iOS | 웹(Next.js) |
|---|---|
| Xcode 프로젝트 | Next.js 프로젝트 폴더 |
| `Package.swift` / SPM | `package.json` + npm |
| SwiftUI `View` | React `component` (함수 하나 = 화면 조각) |
| `@State` | `useState` |
| `NavigationStack` / 라우팅 | 폴더 구조가 곧 URL (App Router) |
| `URLSession` + 디코딩 | `fetch` + TanStack Query |
| Info.plist / xcconfig | `.env` 파일 (환경변수) |
| 시뮬레이터 빌드 | `npm run dev` → 브라우저 localhost:3000 |
| 컴포넌트 라이브러리(SwiftUI 기본 뷰) | UI 라이브러리(버튼/테이블/모달 완제품) |

**핵심:** 폴더에 파일을 만들면 그게 화면이 되고, 함수 하나가 화면 조각이다.

---

## 1. 기술 스택 (이걸로 고정)

웹 처음 + AI 개발 + 관리자 화면 조건에 최적화한 조합. 고민하지 말고 그대로 사용.

- **Next.js (App Router)** — 프레임워크 (기획서 명시)
- **Mantine** — UI 라이브러리. 테이블/모달/폼/알림(토스트) 전부 완제품 내장. 이 기획서 요구사항에 딱 맞음
- **TanStack Query** — 서버 데이터 조회/캐싱/자동 새로고침 (7.2 "이미 처리된 건 → 목록 새로고침"에 사용)
- **Auth.js (구 NextAuth)** — 구글 로그인 + 이메일 화이트리스트 (9장)

> 대안: Ant Design(ProTable이 정렬/검색/필터 내장). 처음엔 Mantine이 배우기 편함.

---

## 2. 환경 세팅 (약 1.5~2h)

### 2-1. 사전 설치 (한 번만)
1. **Node.js LTS** 설치 → https://nodejs.org (LTS 버전)
2. 설치 확인:
   ```bash
   node -v   # v20 이상이면 OK
   npm -v
   ```
3. 에디터는 Cursor / Claude Code 그대로 사용

### 2-2. 프로젝트 생성
```bash
npx create-next-app@latest spot-admin
```
프롬프트 답:
- TypeScript → **Yes**
- ESLint → **Yes**
- Tailwind CSS → **No** (Mantine 쓸 거라 불필요)
- `src/` directory → **Yes**
- App Router → **Yes**
- Turbopack → **Yes**
- import alias → 기본값(Enter)

### 2-3. 라이브러리 설치
```bash
cd spot-admin
npm install @mantine/core @mantine/hooks @mantine/notifications @mantine/dates @tanstack/react-query dayjs
npm install next-auth
```

### 2-4. 개발 서버 켜기
```bash
npm run dev
```
→ 브라우저에서 `localhost:3000` (시뮬레이터 대신 브라우저). 파일 저장하면 자동 새로고침(핫 리로드).

### 2-5. Mantine / Query 초기 설정
`layout.tsx`에 Provider 감싸는 정형 작업 → **AI에게 통째로 위임**:
> "Next.js App Router에 Mantine v7 + TanStack Query Provider 세팅해줘. layout.tsx랑 postcss 설정까지."

**이 단계는 개념만 이해하고 명령어 실행 + AI 위임으로 넘긴다.**

---

## 3. 개발 계획 (15시간 배분)

**핵심 전략 2가지**
1. **목(mock) 데이터로 화면부터 다 만들고**, 서버는 마지막에 갈아끼운다 → 서버 안 기다리고 진행
2. **로그인은 뒤로** 미룬다 → 화면 개발을 막지 않게

### Phase 1 — 뼈대 + 목 데이터 (1.5h)
- 폴더 구조: `/` (리스트), `/spots/[id]` (상세)
- 상태 enum, 데이터 타입 정의 (`검수중 / 재검토대기 / 승인 / 반려`)
- 목 데이터 20건 배열 + 좌측 메뉴/헤더 레이아웃(Mantine `AppShell`)
- **AI 프롬프트:** "이 상태 enum이랑 Spot 타입으로 목 데이터 20건 만들고, Mantine AppShell 레이아웃 잡아줘"
- ✅ 산출물: 빈 껍데기 앱이 localhost에 뜸

### Phase 2 — 리스트 화면 (3h) ← 제일 무거움
- Mantine `Table`로 6개 컬럼(신청일시 / 유저 / 스팟명 / 상태 / 처리자 / 처리일시)
- **복합 정렬:** 미처리 상단 → 재검토 우선 → 오래된 순 / 완료건은 최근 처리순 (기획서 4.1)
- 상태 필터(Segmented/Select), 검색 인풋(스팟명·닉네임 부분일치)
- 행 클릭 → 상세로 이동
- **주의:** 정렬 규칙이 이 화면의 90%. AI가 짠 정렬 함수를 목 데이터로 눈으로 검증할 것
- ✅ 목록이 규칙대로 정렬·검색·필터됨

### Phase 3 — 상세 검수 화면 (2h)
- 등록 필드 전량 표시(사진 / 상세주소 / 한줄코멘트 / 촬영일시 / 필터) (4.2)
- **재검토 건이면 상단에 이전 반려 사유 + 시점 강조 블록**
- (선택) 유저 신뢰도 영역은 자리만 잡아두기 (서버 미확정 — 8장)
- ✅ 한 건 클릭하면 전부 보임

### Phase 4 — 승인/반려 모달 (2.5h)
- 승인: 확인 모달 → 확정 시 상태 변경 (5장)
- 반려: 사유 5택1 모달, **"기타" 선택 시 입력 필수(안 쓰면 확정 버튼 비활성)**, 취소 버튼
- 확정하면 목데이터 상태 바꾸고 목록 갱신
- **AI 프롬프트:** "Mantine Modal로 반려 사유 5택1, 기타 선택 시 textarea 필수입력 validation 걸어줘"
- ✅ 승인/반려가 화면상에서 동작(아직 목데이터)

### Phase 5 — 서버 API 연동 (2.5h)
- 목데이터 호출부를 TanStack Query로 **실제 API 호출로 교체**
- 목록 조회 / 승인 / 반려 3개 API 연결
- 여기서 서버 응답 스펙 안 맞아서 삽질 발생(정상)
- ✅ 진짜 데이터로 동작

### Phase 6 — 로그인 (2h)
- Auth.js + 구글 로그인, `signIn` 콜백에서 **허용 이메일 배열 체크**, 미등록이면 차단 (9장)
- 구글 OAuth 클라이언트 발급(Google Cloud Console) 필요 → 이 과정이 낯섦
- ✅ 등록된 이메일만 진입

### Phase 7 — 예외처리 + QA (1.5h)
- 동시 처리: 서버가 "이미 처리됨" 응답 주면 토스트 + 목록 자동 새로고침 (7.2)
- 네트워크 실패: 실패 토스트 + 원상복구
- 완료기준(10장) 눌러보며 점검
- ✅ 예외 케이스 통과

**합계: 15h** (Phase 2·5·6이 실제 난관, 나머지는 AI가 빠르게 뽑음)

---

## 4. 현실적인 주의 (15h 지키려면)

- **로그인 OAuth 발급**과 **서버 응답 불일치 디버깅**이 시간 잡아먹는 주범. 여기서 막히면 Phase 7 QA 시간을 깎게 됨 → 15h는 "순조로울 때" 기준, 막히면 18~20h.
- 8장 미확정 3개(재신청 방식 / 신뢰도 / 인증방식)가 확정 안 되면 Phase 3·5에서 대기 발생.
- 배포(Vercel 올리기)는 이 15h에 **미포함** — 별도 1~2h.

---

## 5. 서버팀 확인 필요 항목 (착수 전 크로스체크 — 기획서 8장)

| 항목 | 제안 방향 | 확인 필요 |
|---|---|---|
| 유저 신뢰도 정보 | 표시하는 방향 | 가입일 / 누적 등록·승인·반려 횟수 제공 가능 여부 |
| 재신청 처리 방식 | 기존 스팟 이어쓰기 | 리스트 업데이트 방식(기존 건 갱신 vs 신규 건 추가) 기술 검토 |
| 팀원 계정 등록 | 하드코딩(화이트리스트) | 실제 구현 방식 검토 |

**추가로 서버에 요청할 API 규격**
- 목록 조회(정렬/검색/필터를 서버가 처리하는지 여부 → Phase 2 공수에 직결)
- 승인 API
- 반려 API (사유 + 추가설명 + 처리자 + 시점 저장)
- 동시성 처리: 이미 처리된 건이면 conflict(예: 409) 응답 여부 → Phase 7에 직결
